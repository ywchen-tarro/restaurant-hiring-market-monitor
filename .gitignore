__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
venv/
.env

# Local-only runtime
logs/
*.log

# Internal notes — never push, regardless of where the file sits
CLAUDE.md
**/CLAUDE.md

# Local-only dashboard view + benchmark data — never push
local/
**/benchmark.json

# iCloud placeholders
.icloud
*.icloud
